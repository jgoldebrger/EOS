# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.setup.ts >> authenticate as admin
- Location: tests/e2e/auth.setup.ts:10:6

# Error details

```
Test timeout of 45000ms exceeded.
```

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: 'Demo Company' })
Expected: visible
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 60000ms
  - waiting for getByRole('heading', { name: 'Demo Company' })

```

```yaml
- heading "404" [level=1]
- heading "This page could not be found." [level=2]
- region "Notifications alt+T"
- alert
```

# Test source

```ts
  1  | import { mkdirSync } from "node:fs";
  2  | import path from "node:path";
  3  | import { expect, test as setup } from "@playwright/test";
  4  | import { E2E_ADMIN_EMAIL, E2E_ADMIN_PASSWORD, E2E_ORG_SLUG, waitForOrgLanding } from "./helpers/auth";
  5  | import { injectSupabaseSession } from "./helpers/supabase-session";
  6  | 
  7  | const authDir = path.join(__dirname, ".auth");
  8  | const adminAuthFile = path.join(authDir, "admin.json");
  9  | 
  10 | setup("authenticate as admin", async ({ context, page }) => {
  11 |   mkdirSync(authDir, { recursive: true });
  12 | 
  13 |   await injectSupabaseSession(context, E2E_ADMIN_EMAIL, E2E_ADMIN_PASSWORD);
  14 |   await page.goto(`/org/${E2E_ORG_SLUG}/dashboard`);
  15 |   await waitForOrgLanding(page, E2E_ORG_SLUG);
> 16 |   await expect(page.getByRole("heading", { name: "Demo Company" })).toBeVisible({
     |                                                                     ^ Error: expect(locator).toBeVisible() failed
  17 |     timeout: 60_000,
  18 |   });
  19 | 
  20 |   await context.storageState({ path: adminAuthFile });
  21 | });
  22 | 
```